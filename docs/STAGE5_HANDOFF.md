# Этап 5 — что сделано и что осталось

Было веткой `feat/stage5-final-assembly`, слито в `main` через [#5](https://github.com/grouvi25/browser-mmo-90s/pull/5)
04.09.2026. После мержа в `main` легло ещё восемь коммитов напрямую — тем же
днём: замыкание В13, пересчёт фермы/окупаемости объектов и бара, фикс
антимастерства. Веток `feat/stage5-final-assembly` и `fix/logic-hardening`
в репозитории больше нет — обе слиты и удалены, сливать в `main` больше
нечего.
ТЗ: `docs/specs/stage-5/` — мастер плюс четыре приложения, все актуальны.
CI на `main` зелёный, прод стоит на вершине `main` (сверено `git rev-parse
HEAD` на сервере против `origin/main`).

## Сделано

| Шаг | Что | Где смотреть |
|---|---|---|
| G0 | Долги Этапа 4: бой 5×5 играбелен, витрина честная | `territory-battle-e2e.test.ts` |
| G1 | Разделы админки: бригады, районы, заявки, налёты, премиум, единая лента журналов | `src/modules/admin-strategy/` |
| G2 | Обратимость: журнал действий с обратной операцией, откат, цепочка транзакций | `src/modules/admin-actions/` |
| G3 | Антиабуз: граф связей, семь детекторов, два жёстких лимита | `src/modules/antiabuse/` |
| G4 | Сквозной прогон баланса; сценарий антимастерства прогнан впервые 04.09.2026 — нашёл реальный баг (задвоенный `antiSkillLevel` в `calcRawDamage`), исправлен | `scripts/simulate-full.ts`, `scripts/simulate-antimastery.ts` |
| G5 | Исполняемая приёмка, 19 из 19 исполненных | `npm run accept:stage5` |
| G7 | Четыре документа для заказчика: запуск, админка, системы, ограничения версии | `docs/RELEASE_LAUNCH.md`, `RELEASE_ADMIN.md`, `RELEASE_SYSTEMS.md`, `RELEASE_LIMITATIONS.md` |
| G8 | Импортный инструмент — 4-я ступень крафта, +15% к скорости цикла, цена подтверждена заказчиком (В7) | `rcp_import_tool` |
| — | Фронт админки: вход, семь разделов | `frontend/src/pages/admin/` |

Команды:

```bash
cd backend
npm run accept:stage4     # 22 из 22
npm run accept:stage5     # 19 из 19 исполненных, 5 отложено
npx tsx ../scripts/simulate-full.ts --days 30 --seed 4242
```

Локальный админ для проверки фронта: `devadmin` / `devadmin_pass_123`
(заведён только в dev-базе).

## Осталось

| Шаг | Что | Блокирует |
|---|---|---|
| G6 | Фактическая проверка отката доведена не до конца: бэкап прода восстановлен в отдельную базу, мигрирован вперёд, реальный старый образ поднят против неё и остался здоровым — это доказывает нужное свойство, но **буквальный bash-сценарий отката из `cd.yml` ни разу не воспроизведён шаг за шагом**, и `accept:stage4`/`accept:stage5` гонялись через `tsx` из чекаута, а не на собранном релизном docker-образе backend | ничего |
| G9 | Закрытый тест не проведён. Черновик приглашения готов (`docs/STAGE5_CLOSED_TEST_INVITE_DRAFT.md`), но не закоммичен, плейсхолдеры даты окончания и контакта для багрепортов не заполнены | набор людей |

Плюс антиабуз остаётся частичным — семь детекторов из четырнадцати, `HELPER_DRAIN`
и `WAR_COLLUSION` не реализованы: упираются в данные, которых пока нет и
появятся только на закрытом тесте (G9). Список отложенных проверок приёмки —
в `docs/stage5-acceptance-report.json` поимённо с причиной.

## Открытые вопросы к заказчику

Закрыты все. **В13** (коридор владельца объекта) закрыт 04.09.2026: расход
«содержание объекта 150 ₽/сутки», который вычитался из прибыли владельца,
оказался придуман прямо в симуляторе — в игре такого стока нет. После
удаления коридор сошёлся сам, без единой правки баланса: 124% против
требуемых 120–160%.

Следом в тот же день заказчик решением закрыл и оставшиеся три несошедшихся
коридора — ферму (90% → 32% честно, затем 67% после правки выхода картофеля)
и окупаемость объектов/бара (цены объектов и бара пересчитаны под модель
В11 «владелец работает сам»). Все девять коридоров сходятся, проверено
исполняемой приёмкой (`accept:stage5`, проверка 18 — реальный подпроцесс
`simulate-full.ts`, не бумага). Подробности и вся цепочка рассуждений —
`docs/STAGE5_FULL_RUN_REPORT.md`.

## Что важно знать, прежде чем трогать код

- **Приёмка исполняемая, не бумажная.** В Этапе 4 бумажный протокол выглядел
  покрытым тестами, а исполненный нашёл три настоящих дефекта — включая бой
  за территорию, который не запускался вовсе. Не ослабляйте проверку под
  факт: если она красная, либо чините код, либо выносите вопрос.
- **Действие без обратной операции админу не выдаётся.** Не «потом
  придумаем», а такой кнопки не будет. Пример: гашение заявки с возвратом
  взноса отменить нечем, поэтому его нет.
- **Частичный уникальный индекс** на открытую заявку живёт в
  `prisma/raw-indexes.sql`: Prisma не умеет объявлять его в схеме, а CI
  поднимает базу через `db push`. После `db push` обязателен
  `npm run db:raw-indexes` — иначе уникальность заявки исчезает молча.
- **Проверяйте по коду возврата, а не по хвосту вывода.** Я один раз прочитал
  последнюю строку линта (она пустая) и увидел ошибку только в CI.
- **Числа в симуляторах не самоочевидны.** В13 и фикс фермы — оба про
  константы, придуманные прямо в скрипте прогона и никогда не сверенные
  с реальным сидом или кодом. Перед тем как доверять несошедшемуся
  коридору, сверяйте формулу симулятора с `src/` и `prisma/economy-data.ts`,
  а не только с ТЗ.
